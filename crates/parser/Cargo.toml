[package]
name = "mythoside-parser"
version = "0.1.0"
edition = "2021"
description = "Markdown and YAML frontmatter parser for MythosIDE core"

[dependencies]
mythoside-narrative = { path = "../narrative" }
serde = { version = "1", features = ["derive"] }
serde_norway = "0.9.42"
