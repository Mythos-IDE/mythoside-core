[package]
name = "mythoside-common"
version = "0.1.0"
edition = "2021"
description = "Shared utilities and helpers for MythosIDE core"

[dependencies]
chrono = { version = "0.4.45", features = ["serde"] }
